#!/usr/bin/env bash
#
# Stop the local preview server. The logic lives in lab-serve so there
# is only one implementation to maintain.

set -euo pipefail

exec "$(dirname "${BASH_SOURCE[0]}")/lab-serve" stop
